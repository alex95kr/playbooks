---
home_dir: "{{ ansible_env.HOME }}"
django_cms: django_cms
full_path: "{{ home_dir }}/{{ django_cms }}"
django_venv: django_venv
django_project: django_project
server: "{{ hostvars['web'].ansible_enp0s8.ipv4.address }}{{ port }}"
port: 8800
